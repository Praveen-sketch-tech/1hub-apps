# Test Calculator App — generated by AI App Importer

App slug: test-calculator-app
App number: 035
Route: /apps/test-calculator-app

## Install into your 1Hub Apps repo

1. Extract this zip into the root of your project so files merge into "src/apps/test-calculator-app/".
2. Replace src/core/apps/appRegistry.ts with the copy included in this zip (or merge the new entry by hand).
3. Replace src/core/apps/appLoaders.ts with the copy included in this zip (or merge the new lazy route by hand).
4. Run "npm run build" to confirm everything compiles.

No GitHub token, Vercel token, or webhook is required for this local package — those are
only needed if you use the optional "Push & Deploy" button instead.

## What was auto-generated
- Auto-registered "test-calculator-app" in src/core/apps/appRegistry.ts as App #035.
- Auto-registered a lazy route for "test-calculator-app" in src/core/apps/appLoaders.ts.
